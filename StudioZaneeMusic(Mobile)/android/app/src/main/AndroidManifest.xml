<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.duanbai3">

    <!-- 🌐 Quyền truy cập Internet cho Google Maps và API -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- 📍 Cho phép truy cập vị trí (nếu bạn muốn hiển thị vị trí người dùng trên map) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:label="duanbai3"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <!-- 🗺️ Thêm API Key Google Maps tại đây -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCLRXWA_W-JCy-gxKH3rrFji118g2MKWw4" />

        <!-- ⚙️ Cấu hình Activity chính của Flutter -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <!-- Hiển thị theme khi khởi động -->
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <!-- Màn hình khởi chạy -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ⚙️ Flutter embedding -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <!-- 🔍 Cấu hình truy vấn cho Text Processing (Flutter yêu cầu) -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>
    </queries>
</manifest>
